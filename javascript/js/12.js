// Elabora una función de expresión que calcule el area de una figura geometrica

const areaRectangulo = function(base, altura){
    console.log(altura * base);
}
areaRectangulo(10,10);


//Declarar una función 

const areaRec(base, altura){
    return base*altura;
}

areaRec(10,10)