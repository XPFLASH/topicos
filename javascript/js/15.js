//Operador Ternario
// condicion ?_si es verdadero_ : _si es falso_;

const numero = 40;

const valor = numero> 30 ? 'El numero es mayor a 30' : 'El numero es menor';

console.log(valor);

const edad = 10;

const valorEdad = edad === 1 ? `Tienes ${edad} año` : `Tienes ${edad} años`;

console.log(valorEdad)